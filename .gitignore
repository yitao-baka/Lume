node_modules
dist
.claude/
# Local test scratch files (clipboard preview testing)
test/
# tauri externalBin staging (generated by scripts/package-zip.sh from the
# release build; kept out of git because it's a rename of build output)
src-tauri/binaries/
